name: Build APK

on:
  workflow_dispatch:
  push:
    tags: ['v*']

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine version
        id: version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            TAG="${GITHUB_REF#refs/tags/}"
          else
            LATEST=$(git tag -l 'v*' --sort=-v:refname | head -1)
            if [ -z "$LATEST" ]; then
              TAG="v1.0.0"
            else
              IFS='.' read -r major minor patch <<< "${LATEST#v}"
              TAG="v${major}.${minor}.$((patch + 1))"
            fi
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "name=${TAG#v}" >> "$GITHUB_OUTPUT"
          echo "code=$(date +%s)" >> "$GITHUB_OUTPUT"

      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Generate icons from repo assets
        run: |
          sudo apt-get install -y imagemagick
          mkdir -p android/app/src/main/res/mipmap-{hdpi,xhdpi,xxhdpi,xxxhdpi}
          convert icon-512.png -resize 72x72   android/app/src/main/res/mipmap-hdpi/ic_launcher.png
          convert icon-512.png -resize 96x96   android/app/src/main/res/mipmap-xhdpi/ic_launcher.png
          convert icon-512.png -resize 144x144 android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png
          convert icon-512.png -resize 192x192 android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png
          # Adaptive-icon foreground (API 26+): center-crop to 84% so the art
          # bleeds to the mask edge — the launcher then cuts its circle out of
          # the artwork instead of shrinking the whole square into a white disc.
          convert icon-512.png -gravity center -crop 430x430+0+0 +repage /tmp/fg.png
          convert /tmp/fg.png -resize 162x162 android/app/src/main/res/mipmap-hdpi/ic_launcher_foreground.png
          convert /tmp/fg.png -resize 216x216 android/app/src/main/res/mipmap-xhdpi/ic_launcher_foreground.png
          convert /tmp/fg.png -resize 324x324 android/app/src/main/res/mipmap-xxhdpi/ic_launcher_foreground.png
          convert /tmp/fg.png -resize 432x432 android/app/src/main/res/mipmap-xxxhdpi/ic_launcher_foreground.png

      - name: Decode persistent signing key
        working-directory: android
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > release.keystore

      - name: Setup Gradle wrapper
        working-directory: android
        run: |
          gradle wrapper --gradle-version 8.5

      - name: Build release APK
        working-directory: android
        env:
          KEYSTORE_PATH: release.keystore
          KEYSTORE_PASS: ${{ secrets.KEYSTORE_PASS }}
          KEY_ALIAS: release
          KEY_PASS: ${{ secrets.KEYSTORE_PASS }}
          VERSION_CODE: ${{ steps.version.outputs.code }}
          VERSION_NAME: ${{ steps.version.outputs.name }}
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: Rename APK
        run: |
          cp android/app/build/outputs/apk/release/app-release.apk "debt-freedom-${{ steps.version.outputs.tag }}.apk"
          # Stable-named copy so releases/latest/download/debt-freedom.apk always works
          cp android/app/build/outputs/apk/release/app-release.apk debt-freedom.apk

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: debt-freedom-apk
          path: debt-freedom-${{ steps.version.outputs.tag }}.apk

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="${{ steps.version.outputs.tag }}"
          # gh creates the tag via the API; git-pushing tags is rejected when
          # the tagged commit's workflow files differ from the default branch
          if ! gh release view "$TAG" >/dev/null 2>&1; then
            gh release create "$TAG" "debt-freedom-${TAG}.apk" debt-freedom.apk \
              --target "$GITHUB_SHA" \
              --title "Debt Freedom $TAG" \
              --generate-notes
          fi
